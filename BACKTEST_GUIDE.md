# 📊 DOPAMING-STOCKBOT 백테스팅 실행 가이드

이 문서는 내장 백테스팅 엔진과 데이터 수집기를 사용하는 상세 방법을 설명합니다.

---

## 1. 전제 조건 (Prerequisites)
- **32비트 가상환경**: 반드시 `venv32` 환경이 활성화되어 있어야 합니다. (키움 API 연동 필수)
- **로그인**: 키움 OpenAPI가 로그인된 상태여야 합니다.

---

## 2. 데이터 수집 (Data Collection)
백테스트를 위한 과거 1분봉 데이터를 수집합니다.

```powershell
# 프로젝트 루트 디렉토리에서 실행
python tools/data_fetcher.py
```

- **설정 변경**: 수집할 종목 리스트는 `tools/data_fetcher.py` 파일 하단의 `target_stocks` 변수에서 수정 가능합니다.
- **저장 위치**: `data/historical/{종목코드}_1m.csv`
- **안정성**: 현재 초당 1건 내외로 속도가 제한되어 있으며, 중간 저장(Autosave) 기능이 활성화되어 있습니다.

---

## 3. 백테스트 실행 (Backtest Execution)
수집된 CSV 데이터를 기반으로 전략의 수익률을 시뮬레이션합니다.

```powershell
# 프로젝트 루트 디렉토리에서 실행
python run_backtest.py
```

- **결과 보고**: 총 수익률, 최종 자산, 거래 횟수가 콘솔에 출력됩니다.
- **세부 설정**:
    - 슬리피지 및 수수료: `backtest/virtual_broker.py`
    - 익절/손절 타겟: `backtest/engine.py`

---

## 4. 트러블슈팅 (Troubleshooting)

### ⚠️ 에러 코드 -300 (연속조회 제한)
- **원인**: 너무 짧은 시간에 많은 데이터를 요청하여 서버에서 일시적으로 차단된 경우입니다.
- **대응**:
    1. 프로그램을 종료하고 **약 5~10분 정도 대기**합니다.
    2. 키움 트레이 아이콘 우클릭 -> **'계좌 비밀번호 저장'** 메뉴에서 비밀번호가 등록/저장되어 있는지 확인합니다.
    3. 다시 실행해도 발생한다면, `data_fetcher.py`의 `monitor_timer` 간격을 더 늘려주세요.

---

## 5. 로드맵 (Future Roadmap)
- [ ] 틱/초 단위 정밀 백테스팅 구현
- [ ] 멀티 전략 시뮬레이션 지원
- [ ] 백테스트 결과 시각화 차트 추가
